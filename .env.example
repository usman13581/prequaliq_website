# PostgreSQL — website + future AI metadata (separate from prequaliq_db app)
DATABASE_URL=postgresql://prequaliq_website_app:YOUR_PASSWORD@localhost:5432/prequaliq_website

# Production (Railway): add a Postgres plugin, then set DATABASE_URL in service variables.
# Run once after deploy: npm run db:push
